NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# generate with openssl rand -hex 32
PORTAL_API_KEY=
# absolute path to the Obsidian knowledge vault (used by `npm run sync-vault`)
VAULT_PATH=
# dashboard base URL for `npm run log-session` (defaults to the deployed URL)
DASHBOARD_URL=
